/**
 * Setup file for E2E tests
 *
 * This file runs before all E2E tests.
 * Use it to configure the test environment.
 */

// Increase timeout for E2E tests
jest.setTimeout(30000);

// Optional: Global setup for database, seeding, etc.
// beforeAll(async () => {
//   // Setup code
// });

// afterAll(async () => {
//   // Cleanup code
// });
